function [ P_xy ] = P( a , b)
% Evalúa al polinomio de interpolación de Lagrange en (a,b) que aproxima a la
% función f(x,y) definida a continuación.
% P(x,y) = P(a,b) => a=x, b=y

x = [0 1 2];
y = [0 2 4];

suma = 0;

for i = 1:1:3
    for j = 1:1:3
        prod = X_i(a,x,i) * Y_j(b,y,j) * f(x(i),y(j));
        suma = suma + prod;
    end
end

P_xy = suma;

end